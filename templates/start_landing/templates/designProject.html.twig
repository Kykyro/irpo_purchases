<section data-id="design_projects" class="ed-section pb-0">
    <div class="row">
        <div class="col-md-10 col-md-offset-1">

            <h2 class="text-center text-white">Дизайн-проекты пространств образовательно-производственных центров (кластеров).</h2>
            <div class="divider divider-alt divider-center divider-light"></div>
            <p class="lead text-center text-white">Брендбук
                <a href="{{ path('app_download_brandbook') }}" >
                    <button class="btn btn-sm">
                        <div class="fa fa-download"></div>
                        Скачать
                    </button>
                </a>
            </p>
            <p class="lead text-center text-white">Памятка по созданию дизайн-проектов в рамках федерального проекта «Профессионалитет»
                <a href="{{ path('app_download_memo_file') }}">
                    <button class="btn btn-sm">
                        <div class="fa fa-download"></div>
                        Скачать
                    </button>
                </a>
            </p>
            <p class="lead text-center text-white">Ссылка на материалы Брендбука
                <a href="https://disk.yandex.ru/d/PETEvtYtXSOMyg" target="_blank">
                    <button class="btn btn-sm">
                        <div class="glyphicon glyphicon-cloud-download"></div>
                        Просмотреть
                    </button>
                </a>
            </p>

        </div>
    </div>
    <div class="container" style="margin-bottom: 3%">
        <div id="portfolio-gallery" class="ed-carousel ed-slick dots-classic dots-light dots-outside portfolio-gallery">
            {% for i in designProjectExample %}

                <article class="carousel-item portfolio-item web">
                    <div class="project-container mb-0 scale_img">
                        <a href="{{ asset('/uploads/designProjectExample/'~i.getPhoto()) }}" data-size="3219x2081" class="portfolio-gallery__img--main">
                            <img src="{{ asset('/uploads/designProjectExample/'~i.getPhoto()) }}" alt>
                            <div class="overlay">
                                <div class="overlay-wrapper">
                                    <div class="overlay-inner background-dark-5 opacity-70"></div>
                                    <div class="overlay-inner overlay-content"><i class="fa fa-search" aria-hidden="true"></i></div>
                                </div>
                            </div>
                        </a>
                        <div class="project-details text_scale_img" style="height: 220px">
                            <div style="display: flex; align-items: center; justify-content: center; height: 75%">
                                <h4 class="h5 project-title heading-uppercase" style="font-size: 13px"> {{ i.getName() }}</h4>
                                <p style="margin-bottom: 10%">{{ i.getDescription() }}</p>
                            </div>
                            {% if i.getFile() is not null %}
                            <a href="{{ path('app_download_design_project_presentation_file', {'file': i.getFile()}) }}" >
                                <button class="btn btn-sm" id="download-button">
                                    <div class="glyphicon glyphicon-cloud-download"></div>
                                    Скачать
                                </button>
                            </a>
                            {% endif %}
                        </div>
                    </div>

                </article>

            {% endfor %}
        </div>
    </div>
</section>