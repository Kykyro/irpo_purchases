{% extends "journalist/index.html.twig" %}




{% block content %}
<div class="row">
    <div class="col-sm-8 offset-sm-2 ">
        <div class="wrapper wrapper-content ">
            <div class="ibox-content block-shadow ">
                <div class="row">
                    <a href="{{ path('app_list_article') }}">
                        <button class="btn btn-gray-all m-3">
                            Назад
                        </button>
                    </a>
                </div>
                <div class="row d-flex justify-content-center">
                    <h1>
                        <b>{{ article.getTitle() }}</b>
                    </h1>
                </div>

                <div class="row d-flex justify-content-center">
                    <h1>
                        <img src="{{ asset('/uploads/articleTitles/' ~ article.getImg()) }}" alt="нет картинки" class="m-3" style="width: 670px"></td>
                    </h1>
                </div>

                <div class="row d-flex justify-content-center">
                    <div class="col-md-8">
                        {{ article.getContent()| raw }}
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}