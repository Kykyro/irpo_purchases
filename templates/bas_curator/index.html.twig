{% extends 'default/base.html.twig' %}

{% block content %}
    <div class="row mt-3">

            {% for i in bas %}
                {{ include('bas_curator/bas_card.html.twig') }}
            {% endfor %}

    </div>
    <div class="navigation">
        {{ knp_pagination_render(bas, 'default/twitter_bootstrap_v3_pagination.html.twig') }}
    </div>
{% endblock %}
