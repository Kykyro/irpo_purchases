<!DOCTYPE html>
<html>

<head>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">

    <title>ИРПО Закупки</title>

    <link rel="icon" type="image/x-icon" href="{{ asset('img/ico/rpop_icon.png') }}">

    <link href="{{ asset('css/bootstrap.min.css') }}" rel="stylesheet">
    <link href="{{ asset('font-awesome/css/font-awesome.css') }}" rel="stylesheet">

    <!-- Toastr style -->
    <link href="{{ asset('css/plugins/toastr/toastr.min.css') }}" rel="stylesheet">

    <!-- Gritter -->
    <link href="{{ asset('js/plugins/gritter/jquery.gritter.css') }}" rel="stylesheet">

    <link href="{{ asset('css/animate.css') }}" rel="stylesheet">
    <link href="{{ asset('css/style.css') }}" rel="stylesheet">
    <link href="{{ asset('css/my_style.css') }}" rel="stylesheet">
    <link  href="{{ asset('./js/cropper/cropper.css') }}" rel="stylesheet">
    <script src="{{ asset('js/jquery-3.1.1.min.js') }}"></script>
</head>

<body class="left-bar-color-bg">
<div id="wrapper" class="left-bar">
{#    правое меню#}
    <nav class="navbar-default navbar-static-side" role="navigation">
        <div class="sidebar-collapse">
            <ul class="nav metismenu left-bar" id="side-menu">
                <li class="nav-header">
                    <div class="dropdown profile-element">
                        {% if is_granted('ROLE_REGION') %}
                            <a href="{{ path("app_profile") }}" >
                                <span class="block m-t-xs font-bold left-bar-color-text">{{ app.user.getUsername() }}</span>

                                {% if  app.user.getUserInfo().getRfSubject().getName() is  defined %}
                                    <span class="small text-xs block">{{ app.user.getUserInfo().getRfSubject().getName() }} </span>
                                {% else %}
                                    <span class="small text-xs block">?</span>
                                {% endif %}

                            </a>
                        {% else %}
                            <span class="block m-t-xs font-bold left-bar-color-text">{{ app.user.getUsername() }}</span>
                        {% endif %}

                    </div>
                </li>
                {% if is_granted('ROLE_REGION') %}
                    <li>
                        <a href="{{ path("app_main") }}"><i class="fa fa-home left-bar-color-text"></i> <span class="nav-label left-bar-color-text">Добавление закупок</span></a>
                    </li>
                    <li>
                        <a href="{{ path("app_region_is") }}"><i class="fa fa-table left-bar-color-text"></i> <span class="nav-label left-bar-color-text">Инфраструктурный лист</span></a>
                    </li>
                {% endif %}
                {% if is_granted('ROLE_INSPECTOR') %}
                    <li > {# class="active" #}
                        <a href="{{ path("app_inspector_main") }}"><i class="fa fa-archive left-bar-color-text"></i> <span class="nav-label left-bar-color-text">Закупки</span></a>
                    </li>
                    <li > {# class="active" #}
                        <a href="{{ path("app_feedback") }}"><i class="fa fa-at left-bar-color-text"></i> <span class="nav-label left-bar-color-text">Обратная связь</span></a>
                    </li>
                    <li > {# class="active" #}
                        <a href="{{ path("app_inspector_infrastructure_sheet") }}"><i class="fa fa-table left-bar-color-text"></i> <span class="nav-label left-bar-color-text">Инфраструктурные листы</span></a>
                    </li>
                    {#<li > class="active"
                        <a href="#"><i class="fa fa-archive left-bar-color-text"></i> <span class="nav-label left-bar-color-text">Закупки [2]</span></a>
                    </li>#}
                {% endif %}
                {% if is_granted('ROLE_JOURNALIST') %}
                    <li >
                        <a href="{{ path("app_journalist") }}"><i class="fa fa-compass left-bar-color-text"></i> <span class="nav-label left-bar-color-text">Редактор главной страницы</span></a>
                    </li>
                {% endif %}
                {% if is_granted('ROLE_ADMIN') %}
                    <li >
                        <a href="{{ path("app_admin") }}"><i class="fa fa-wheelchair-alt left-bar-color-text"></i> <span class="nav-label left-bar-color-text">Админ панель</span></a>
                    </li>
                {% endif %}



            </ul>

        </div>
    </nav>

    <div id="page-wrapper" class="gray-bg dashbard-1">
        <div class="row border-bottom">
            <nav class="navbar navbar-static-top upper-menu" role="navigation" style="margin-bottom: 0">

                <ul class="nav navbar-top-links navbar-right offset-lg-11">
                    <li>
                        <a href="{{ path("app_logout") }}">
                            <i class="fa fa-sign-out"></i> Выйти
                        </a>
                    </li>
                </ul>

            </nav>
        </div>
