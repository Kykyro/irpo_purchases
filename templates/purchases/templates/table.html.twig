<div class="row">
    <div class="col-lg-10 offset-lg-1">
        <div class="wrapper wrapper-content">

            <div class="row">
                <table class="table block-shadow white-bg">
                    <thead>
                        <tr>
                            <th>№</th>
                            <th>Код</th>
                            <th>Объект закупки</th>
                            <th>Заказчик</th>
                            <th>ГРБС</th>
                            <th>Статус</th>

                        </tr>
                    </thead>
                    <tbody>

                    {% for i in range(low=1, high=10, step=1) %}
                        <tr class="table-item" href="{{ path("app_purchases_detail", {id: i}) }}">
                            <td>Пункт {{ i }}</td>
                            <td>@mdo</td>
                            <td>@mdo</td>
                            <td>@mdo</td>
                            <td>@mdo</td>
                            <td>@mdo</td>
                        </tr>
                    {% endfor %}




                    </tbody>
                </table>
            </div>
        </div>

    </div>
</div>