<div class="row">
    <div class="col-sm-8 offset-sm-2 ">
        <div class="wrapper wrapper-content ">
            <div class="ibox-content block-shadow">
                {{ form_start(form) }}
                    <div class="form-group  row">
                        <label class="col-sm-2 col-form-label text-right">Год</label>
                        {{ form_widget(form.year) }}
                    </div>
                    <div class="form-group  row">
                        <label class="col-sm-2 col-form-label text-right">Регион</label>
                        {{ form_widget(form.rf_subject) }}
                    </div>

                    <div class="form-group row ">
                        <div class="col-lg-12">


                            <button class="col-lg-2  btn btn-info btn-lg pull-right m-1" type="submit">
                                <nav class="fa fa-search "></nav>
                                Найти
                            </button>

                        </div>
                    </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
</div>