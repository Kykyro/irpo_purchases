<div class="row">
    <div class="col-lg-10 offset-lg-1">
        <div class="wrapper wrapper-content">

            <div class="row">
                {% if  purchases is defined%}
                <table class="table block-shadow white-bg">
                    <thead>
                        <tr>
                            <th>id</th>
                            <th>Код</th>
                            <th>Объект закупки</th>
                            <th>Заказчик</th>
                            <th>ГРБС</th>
                            <th>Статус</th>

                        </tr>
                    </thead>
                    <tbody>

                        {% for i in purchases %}
                            <tr class="table-item" href="#">
                                <td>Пункт </td>
                                <td>@mdo</td>
                                <td>@mdo</td>
                                <td>@mdo</td>
                                <td>@mdo</td>
                                <td>@mdo</td>
                            </tr>
                        {% endfor %}





                    </tbody>
                </table>
                {% endif %}

            </div>
        </div>

    </div>
</div>